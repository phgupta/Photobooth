1. Implement delete_label function.
2. Check for any small error cases not yet found.
    1. Clicking favorites button doesn't work after the first time.
    2. Position of progress bar - The progress bar should only appear when the user uploads a new image. Right now, it appears anytime we load the page, and anytime we add a new image to the page, even though the image is on the server. It should ONLY appear when uploading images. Look for more cases where the progress bar appears and remove them.
    3. Pressing enter on label_form crashes everything.
3. Photobooth Part 2.
